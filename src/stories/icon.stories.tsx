import React from "react";

import { LockIcon, LetterIcon, ArrowIcon } from "../components/icons";

export default {
    title: "icons",
}

export const Lock_Icon = () => (
    <LockIcon/>
)
export const Letter_Icon = () => (
    <LetterIcon/>
)
export const Arrow_Icon = () => (
    <ArrowIcon/>
)